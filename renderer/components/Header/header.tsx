import { Button, Container } from '@electron/ui'

const Header = () => {
  return (
    <header>
      <Container>
        <Button type="primary">Header</Button>
      </Container>
    </header>
  )
}

export default Header
